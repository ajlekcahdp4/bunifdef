import sys
import os
import lit.llvm

config.obj_root = "@BINARY_DIR@"
config.src_root = "@SOURCE_DIR@"

lit.llvm.initialize(lit_config, config)
lit_config.load_config(config, os.path.join(config.src_root, "test/lit.cfg.py"))
config.suffixes = [ ".c", ".test" ]
